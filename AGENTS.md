Keep your changes as low impact as possible. You do not need to give me a summary of changes. Try to reference other parts of the codebase to ensure your changes are consistent with the existing code style and practices. Keep your responses concise and focused.

Do not commit, push code or create PRs unless explicitly asked. Do not add co-authorship attribution of any kind.

Read all context and instructions carefully before making changes. Code may be manually modified between messages. Do not suggest code that has been deleted or is no longer relevant.

Do not modify or add any code that sends WRITEs to unified-socials-db without explicit approval.

Do not add comments unless they are absolutely necessary for clarity. Your code should describe what it does, not comments. If you do add comments, ensure they are clear, concise, and relevant to the code they accompany (with a goal of under 100 characters for all comments). Do not add huge blocks of comments.

If asked to change the requirements or behavior of a feature, make sure previous implementations that you suggested are also updated to reflect the new requirements. Always ask questions when needed.

File names should follow the following guidelines:
- All lowercase
- `_` as space separator for file names
- `-` as a space separator for dates in file names
- Dates in `YY-MM-DD` format

Do not ever write to unified-socials-db yourself for tests etc., and do not touch any code related to actually writing to unified-socials-db without explicit instruction.

Do not leave dev servers running after prompt completion.

Do not touch README.md.

Only call Claude in Chrome / other visual inspection / tests when absolutely necessary.