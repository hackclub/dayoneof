# dev = local tunnel + test Airtable/Slack, prod = the deployed site. Defaults to dev when unset.
APP_ENV=dev

# Every var below may be suffixed _DEV or _PROD. The suffixed value wins when it matches APP_ENV;
# the unsuffixed name is the shared fallback for anything that's the same in both.

# Dev is whatever tunnel fronts `npm run dev` — a Tailscale Funnel host keeps it stable:
# PUBLIC_SITE_URL_DEV=https://<machine>.<tailnet>.ts.net
PUBLIC_SITE_URL_DEV=
PUBLIC_SITE_URL_PROD=

# Hosted GoatCounter site, e.g. https://dayoneof.goatcounter.com — blank turns tracking off. Dev
# stays blank: a Funnel host isn't localhost, so count.js would report local browsing as real.
PUBLIC_GOATCOUNTER_URL_DEV=
PUBLIC_GOATCOUNTER_URL_PROD=

SLACK_BOT_TOKEN_DEV=xoxb-...
SLACK_BOT_TOKEN_PROD=xoxb-...
SLACK_SIGNING_SECRET_DEV=
SLACK_SIGNING_SECRET_PROD=
SLACK_SUBMISSION_CHANNEL_ID_DEV=
SLACK_SUBMISSION_CHANNEL_ID_PROD=
SLACK_ANNOUNCE_CHANNEL_ID_DEV=
SLACK_ANNOUNCE_CHANNEL_ID_PROD=

# Per-environment on purpose: a shared SESSION_SECRET means a cookie signed locally is valid on
# prod, and a shared CRON_SECRET means the copy on your laptop can fire prod's nightly jobs.
SESSION_SECRET_DEV=
SESSION_SECRET_PROD=
CRON_SECRET_DEV=
CRON_SECRET_PROD=

# Where the hourly backup job writes Airtable snapshots. Prod points it at the Orchard volume.
BACKUP_DIR=backups

# One HCA app serves both environments — it just has to list both callback URLs as redirect URIs.
HCA_CLIENT_ID=
HCA_CLIENT_SECRET=

# One base holds both environments. Dev reads and writes the `_dev` copy of each table
# (participants_dev, days_dev, ...) — the table name carries the split, not the base id.
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=

UNIFIED_SOCIALS_TOKEN=
UNIFIED_SOCIALS_API_URL=

# Submitting an untracked post to unified-socials-db starts paid work, so it is off unless this is
# exactly "true". Resolves per-environment like everything else, so setting only
# UNIFIED_SOCIALS_TRACK_POSTS_PROD=true leaves dev unable to spend anything. The token also needs
# `Can start tracking posts` ticked, or the API refuses with 403 and no work starts.
UNIFIED_SOCIALS_TRACK_POSTS=false
# Set to exactly "true" by hand once the submissions channel reaches 200 members.
SUBMISSIONS_OPEN=false
MAX_POST_AGE_DAYS=1
ADMIN_SLACK_IDS=
